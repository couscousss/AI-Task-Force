import type { ArrangementTeam, Evaluation, SolverParams, SolverParticipant } from './types';

export function evaluateArrangement(
  _participants: SolverParticipant[],
  _teams: ArrangementTeam[],
  _params: SolverParams,
): Evaluation {
  throw new Error('not implemented');
}
